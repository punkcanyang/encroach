# AI 开发者自我约束协定

**规则 A (精准读取)**： 除非使用者明确指定（使用 @档名），或是要求全部扫描，或者该档案与当前 Phase 的核心逻辑绝对相关，否则禁止扫描整个专案目录。

**规则 B (增量修改)**： 在修改程式码时，请善用你的 Local Cache 记忆，只输出「需要修改或新增的 Function 片段」，绝对禁止毫无意义地重印整份没有改动的脚本。

**规则 C (模型分工)**： 若任务是「资料结构产生」或「占位符场景建立」，请提醒使用者切换为 Gemini 模型；若是「核心决策树」或「复杂演算法」，则使用 Claude 模型。
